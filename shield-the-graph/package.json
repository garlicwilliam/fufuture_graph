{
  "name": "shield-the-graph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ shield-the-graph",
    "create-local": "graph create --node http://localhost:8020/ shield-the-graph",
    "remove-local": "graph remove --node http://localhost:8020/ shield-the-graph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 shield-the-graph",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.69.1",
    "@graphprotocol/graph-ts": "0.32.0",
    "https-proxy-agent": "^7.0.4"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
